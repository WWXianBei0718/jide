# 「记得」当前项目状态

> 最后更新：2026-07-24（Asia/Shanghai）
> 状态：工程型 Alpha；可进行受控封闭测试，不可公开上线  
> 本文件是代码库当前事实的唯一入口。历史文档与本文件冲突时，以本文件和实际代码为准。

## 当前真实技术状态

- Next.js 15 Pages Router、React、TypeScript。
- Supabase Auth、PostgreSQL、私有 Storage。
- OpenAI Chat Completions，服务端固定为 `gpt-4o-mini`。
- ElevenLabs 声音克隆和 TTS 代码路径存在；仅完成密钥只读验证，未进行付费端到端验收。
- 本地 Git `main` 分支存在；已加入最小权限 GitHub Actions CI 配置和仓库密钥卫生测试，但尚无远程私有仓库，因此 CI 尚未真实运行；自动部署和生产监控仍未配置。
- 本机 `.env.local` 已收紧为仅当前系统用户可读写，测试会在文件存在时阻止过宽权限；AI 和语音接口不再把供应商原始错误体或完整异常对象写入服务端日志。
- 健康检查现在会校验核心服务端配置并使用服务端数据库连接验证就绪状态；失败时返回通用信息，不再暴露数据库错误或样本数据状态。本地真实启动后已只读验证返回 `ready` 和数据库已连接。
- 本轮质量检查已通过：Lint、TypeScript、130/130 单元测试和 Next.js 生产构建全部成功。

## 已验证闭环

- 邮箱密码登录、人物创建、详情、文字资料、私有文件上传与认证接口直传下载。
- 14 份数据库迁移均已部署到独立 Supabase 开发项目；第 8 份消息角色收紧迁移已回读验证，第 9 份语义索引额度迁移已确认包含 `embedding` 分支，第 12 份多模态处理状态迁移已确认 RLS 开启且登录客户端只有读取权限。第 13 份版本化同意迁移已回读确认授权账本只能由服务端写入。第 14 份多模态任务租约迁移已回读确认：登录客户端能读取任务状态但看不到内部 Worker/租约，不能领取或完成任务；三个处理函数仅 Service Role 可执行，并发索引和三条状态约束均已生效。
- 关系型 RLS 已通过双用户越权测试。
- 文件隔离区、基础二进制签名校验、正式私有区发布和跨用户下载拒绝已验证。
- 私有文件预览与下载不再向浏览器返回 Supabase Storage 签名 URL；认证 API 核验用户、状态和文件大小后直接传输，页面只创建本机临时 Blob URL 并主动释放。
- 聊天固定使用真实模型名；默认输出 600 Token，最高 1000 Token。
- 聊天持久限流：每用户每分钟 10 次、每天 100 次；应用返回 429，限流请求不会写消息或调用 OpenAI。
- ElevenLabs 付费接口已接入数据库原子配额：声音克隆每 10 分钟最多 1 次、每天 3 次；TTS 每分钟 20 次、每天 300 次且每天最多 100,000 字符。配额不可用时失败关闭，不调用供应商。开发库已回读确认表、函数和登录用户权限存在，匿名调用返回 401。
- 文字资料新建和语义索引重试已接入数据库原子配额：每 10 分钟最多 20 次、每天 200 次且每天最多 4,000,000 个源文本字符；配额不可用时不会调用 Embedding 服务，资料正文仍可安全保存。
- 声音克隆会拒绝覆盖已有声音模型；供应商创建成功但人物记录保存失败时，会主动撤销新建声音，避免遗留孤立的生物特征资源。TTS 增加 30 秒超时、音频类型和 10MB 大小边界。
- 文件上传授权已接入数据库原子配额：每 10 分钟最多 20 次、每天最多 100 次及 500MB，单文件继续受 25MB 类型规则约束。申请新上传时会顺带认领并清理当前用户最多 50 条已过期隔离文件，降低遗留存储风险。
- 聊天现在携带最近 12 条、最多 6000 字符的对话上下文。
- 人格上下文对资料进行编号，区分直接证据、有限推断和未知内容，并记录上下文版本与使用的资料 ID。
- 已建立完全虚构人物的 40 题固定评测集、本地自动评分、费用硬上限、限速重试和断点续跑。
- 已真实运行 `gpt-5.4-mini` 31/40 题：v1 规则校正后通过 28/31（90.3%），估算成本 $0.034204；剩余题被账户每日请求上限阻断。
- v1 评测已按版本归档；v2 独立回归已尝试，但账户每日请求额度尚未恢复，0 次生成、0 新费用。
- 正式人格验证默认运行完整 40 题；另有覆盖全部 6 类风险的 12 题可选快速检查。必要、能回答明确问题的测试不因几美分成本而缩减。
- 开发库已部署安全的持久化向量表结构和 `match_memory_chunks` 检索函数；新增字段已回读验证，未认证调用会被拒绝。
- 新建文字资料会在服务端切片并尝试用 `text-embedding-3-small` 建立向量；失败会记录明确状态，原始资料仍安全保留。
- 资料管理页会显示语义索引状态；失败时可由用户手动重试，关键词检索在等待额度期间仍可使用。
- 重复索引改为按“资料 + 片段序号”安全更新，并清理已失效的多余片段，避免重试产生重复记忆或唯一键冲突。
- 已增加既有文字资料批量回填工具；默认仅做只读预览，只有显式执行模式才会调用 OpenAI 和写入索引，单次最多处理 100 条并在首次失败后安全停止。
- 已将完全虚构的检索基线扩展到 60 题，其中 50 题用于开发、10 题使用未参与规则调优的留出表达。`hybrid-vector-lexical-v2` 关键词检索总体 Top-1 为 93.3%、Top-3 为 95.0%、MRR 为 0.939；开发集 Top-1 为 100%，但留出集 Top-1 只有 60%、Top-3 只有 70%，因此不能把总体分数误当成真实语义能力。
- 已完成账号数据导出：登录用户可下载结构化 JSON，或包含当前可用私有文件的 ZIP；数据库分页读取，文件逐项验证大小和 SHA-256，响应禁止缓存。同步 ZIP 单次上限为 100 个文件或 100MB，不包含 Embedding 数字。
- 已实现账号删除代码路径：要求密码重新登录，并校验 Supabase JWT `amr` 中 5 分钟内的 `password` 认证记录（自动刷新令牌不算重新验证），同时核对邮箱和固定确认文本；按 ElevenLabs 声音、私有 Storage、数据库、Supabase Auth 顺序清理。账号业务表清理已合并为仅 Service Role 可执行的数据库事务，中途失败会整体回滚，不再留下逐表删除造成的半完成状态。尚未用真实测试账号执行破坏性端到端验收。
- 已修复整个人物删除链路：同样要求密码重新验证和人物姓名确认，先清理 ElevenLabs 声音模型及正式区/隔离区文件，再删除人物数据库记录，避免留下孤立的生物特征和私有文件。
- 聊天已接入 `hybrid-vector-lexical-v2`：有向量时语义检索优先，同时保留关键词检索；人物尚未建立向量时使用明确标记的词法模式。若人物已经有向量但语义服务失败，接口会进入 `memory_retrieval_unavailable` 受限模式并停止生成，不再静默降级后冒险引用错误资料。
- 已建立不接触业务数据的备份恢复基线：14 份版本化迁移均纳入 SHA-256 清单和自动顺序检查，并已全部部署到开发库。常见数据库备份格式已禁止提交进 Git，运行手册已明确数据库与 Storage 必须分别备份。
- 聊天、声音克隆和 TTS 已加入安全请求编号：客户端响应携带 `X-Request-Id`，关键错误日志只记录请求编号、内部事件名、供应商状态码和错误类型，不记录用户 ID、人物 ID、材料、聊天正文或供应商错误正文。
- 关键 API 错误记录已接入无正文、无用户标识的 `security_audit_events` 持久化边界；写入最多等待 1.5 秒，审计存储不可用时保留安全控制台日志且不改变用户请求结果。第 11 份迁移已部署到开发库；远程留存、看板和告警仍未配置。
- 已加入安全审计 90 天保留工具：默认只读预览，只有显式执行命令才会分批删除过期事件；每批最多 500 条、单次最多 10,000 条。尚未接入定时任务。
- OpenAI 聊天和 Embedding 的直连及代理网络路径统一限制为 60 秒；JSON 响应流超过 10MB 会中止，避免供应商异常时长期占用服务进程或无界消耗内存。
- 全站已有 CSP、HSTS、禁止 iframe、禁止内容类型嗅探、Referrer Policy 和 Permissions Policy，并加入自动测试；浏览器只允许连接本应用和 Supabase，不直接连接 OpenAI 或 ElevenLabs。当前 CSP 为兼容 Pages Router 仍允许内联脚本和样式，后续需迁移 nonce/hash。
- `/self-test`、`/test-chat`、`/test-eval` 和项目结构评估接口只在本地开发环境可用，生产构建统一返回 404，避免公开暴露测试操作和内部接口清单。
- 声音状态、人物资料和克隆成功响应不再向浏览器返回供应商 `voice_id`；前端只获得 `voice_ready` 状态。声音状态接口也不再读取整个 ElevenLabs 工作区，只通过 RLS 查询当前用户的人物。
- 账号 JSON/ZIP 导出、账号删除和资料索引失败日志已改为请求编号、固定事件名和错误类型；文件校验失败仍能区分受控原因，但不会记录异常正文、私有文件路径或名称。
- 人物资料接口不再把 Supabase 原始错误正文返回浏览器，数据库结构和内部诊断信息保持在服务端边界内。
- 账号导出格式升级为 `remember-account-export-v4`：包含语音、TTS、上传额度记录和多模态内容处理状态，但不包含 Embedding 数字、ElevenLabs 等供应商资源 ID 或内部错误正文；账号删除会显式清理非级联记录。
- 消息写入边界已收紧：普通登录客户端只能插入 `role=user`，不能伪造 AI/System 历史；AI 回复只在 API 验证人物归属并收到模型结果后由服务端保存。
- 聊天响应只向浏览器返回消息 ID、角色、正文和时间；用户 ID、人物 ID 与检索审计信息继续保存在数据库，不进入页面响应。
- 材料列表和上传完成响应只返回页面展示与后续操作需要的字段，不再把人物归属列、Storage 关联列或整行数据库记录发送给浏览器。
- 文件安全发布状态与内容提取状态已在代码和数据库中分离：图片、音频、视频、PDF 上传成功后只显示“安全保存、等待提取”，不会误报为已经被 AI 理解；任务表只保存受限状态和错误码，不保存供应商原始错误或材料正文。第 12 份迁移已部署并回读确认 RLS、只读客户端权限与仅 Service Role 可执行的上传完成函数；当时开发库中没有需要补建任务的既有就绪非文字资料。OCR、转写和文档解析执行器尚未接入。
- 多模态处理执行边界已加入短期租约、并发锁、超时恢复、最多 20 次受控重试、处理器版本和提取内容哈希。提取文字会写回材料并保留来源信息，但语义索引固定保持 `blocked`，不会在未授权时自动调用 OpenAI。第 14 份迁移已部署并回读权限与约束，实际 OCR、转写和 PDF 解析器仍未接入。
- OpenAI 对话和 Embedding 已接入按人物、告知版本与撤回状态校验：未授权时不会消耗额度、保存待发送消息或调用供应商；文字资料仍会本地保存并明确标为 `blocked`。TTS 会复核当前声音处理授权。对话页可授予和撤回授权，并持续说明回复是 AI 模拟。第 13 份数据库迁移已部署并回读确认权限边界，告知文字仍须在公开上线前完成正式法律复核。
- API 数据库查询已有自动字段白名单门禁，禁止重新引入 `select('*')` 或空字段选择。
- 所有经过统一认证边界的 API 响应（包括认证失败）都会发送 `private, no-store` 与兼容性禁止缓存头，降低共享设备、代理或 CDN 误缓存私人数据的风险。

## 当前聊天事实边界

- 最多扫描 100 条已解析文字资料，按 900 字符左右分片，在 8000 字符预算内选取最多 10 个相关片段。
- 已实现持久化向量语义检索代码和数据库边界，但 OpenAI 账户当前返回 `insufficient_quota`，尚未完成一条真实向量的端到端写入与召回验收。
- 2026-07-23 的索引恢复、状态显示和重复执行测试均使用本地确定性检查，没有调用 OpenAI，也没有将模拟结果记录为真实向量验收。
- 2026-07-23 对 Supabase 开发库执行只读回填预览：扫描到 0 条资料、0 条待回填；未写数据库、未调用 OpenAI、未输出用户资料正文。
- 词法检索可处理开发集中覆盖的明确关键词与同义概念，但 10 题留出集 Top-1 只有 60%、Top-3 只有 70%，不能作为文字对话 Beta 的无感语义后备。已有向量的人物在语义服务失败时会明确进入受限模式。
- 只有已经存在于 `memory_materials.content` 的文字能进入模型上下文。
- 图片、音频、视频和 PDF 可以安全保存，但尚未完成 OCR、转写、解析、切片和向量检索。
- 最近对话用于连续性，但用户在对话里的陈述不会自动升级为已验证人物事实。
- 具体事实要求标注 `[资料N]` 或 `[人物档案]`；没有依据时要求明确说明未记录。
- 人格提示词当前为 `persona-grounding-v2`；新增假设问题推断标注和复用口头禅/原话时的来源要求，尚待额度恢复后回归评测。
- AI 回复审计信息会保存检索版本、候选资料数、选中片段数，以及每个 `[资料N]` 对应的原始资料 ID。

## 数据库迁移

1. `202607150000_initial_schema.sql`
2. `202607150001_secure_multimedia_uploads.sql`
3. `202607150002_harden_relationship_rls.sql`
4. `202607220000_add_chat_rate_limits.sql`
5. `202607220001_secure_memory_chunks.sql`
6. `202607230000_add_external_api_quotas.sql`
7. `202607230001_add_upload_quotas.sql`
8. `202607230002_restrict_message_roles.sql`
9. `202607230003_add_embedding_quotas.sql`
10. `202607240000_atomic_account_deletion.sql`
11. `202607240001_add_security_audit_events.sql`
12. `202607240002_add_material_processing_jobs.sql`
13. `202607240003_harden_versioned_consents.sql`
14. `202607270000_add_material_processing_leases.sql`

## 仍未完成

- **P0 发布阻断：语义检索必须稳定理解同义表达和隐含意图。** 60 题总体 Top-1 已提升到 93.3%，但未参与调优的留出集 Top-1 只有 60%、Top-3 只有 70%；关键词模式仍不能作为文字对话 Beta 的合格无感后备。
- 专用测试邮箱的注册验证邮件完整闭环。
- 完成 `persona-grounding-v2` 的剩余 9 题与回归评测，并进行人工盲评。
- 当前 OpenAI API 项目返回 `insufficient_quota`，因此聊天回归和真实 Embedding 端到端测试暂时无法继续；生产默认模型暂未切换。
- 既有资料的批量向量回填、向量召回质量评测和引用正确率评测。
- 多模态处理任务、RLS、上传原子排队、状态界面、账号导出、安全领取、租约、重试和结果来源边界均已完成并部署。OCR、转写、文档解析器及其真实输出到切片/索引的闭环尚未接入。材料删除时处理任务、提取文字和向量均随材料清理。
- 盲评、跨轮稳定性统计和长期评测历史（当前只保存本地 latest 报告）。
- 超过 100MB 的异步文件导出；账号删除仍需专用测试账号端到端验收，并验证 ElevenLabs 专业声音无法自动删除时的人工支持流程。
- 恶意文件扫描 Worker；生产环境不得开启未扫描文件发布。
- 安装 ZIP 导出依赖后，npm 报告 2 个 high severity 依赖告警；尚未获准向 npm 官方服务发送依赖名称与版本进行审计，来源和修复影响仍待确认，公开发布前必须解决。
- 远程私有 Git 仓库、CI 首次真实运行和自动部署；关键高成本 API 已有安全结构化错误记录，但尚未接入远程错误监控、留存策略和告警。
- AI/声音第三方处理技术门禁及第 13 份数据库迁移均已完成；工程告知文本、供应商 DPA、数据处理区域、默认保留和个人信息跨境影响评估仍未完成，不能把技术门禁视为法律合规完成。
- Supabase Free Plan 不提供付费套餐的每日可恢复备份；当前只有本地迁移完整性门禁，尚未获准导出真实数据库与 Storage，也未在独立临时项目完成恢复演练。

## P0 TODO：语义理解与记忆召回底线

- [ ] OpenAI API 额度恢复后，完成一条虚构资料的真实向量写入、召回和删除闭环。
- [x] 将检索评测扩展到 60 题，覆盖同义词、口语改写、隐含关系、人物称呼、时间和事件追问，并单列 10 题未参与调优的留出集。
- [ ] 使用同一评测集比较关键词、纯向量和混合检索，记录 Top-1、Top-3、MRR、已知资料漏召回率和引用正确率。
- [ ] 继续优化混合排序并用留出集验真，避免泛化标题、高频人物名或针对题目编写的词表制造虚高分数；必要时加入结构化字段过滤和候选片段二次排序。
- [x] 语义服务不可用时明确进入受限模式，不静默退化后继续声称已经理解全部人物记忆。
- [ ] 达到 `PRODUCT_PRINCIPLES.md` 的语义检索闸门，并完成真实规模资料压力测试后，才允许进入文字对话 Beta。

## 下一阶段顺序

1. 按当前开发顺序先继续完成不依赖 OpenAI API 的测试账号、隐私删除、导出、发布与运维基线。
2. 接入图片 OCR、音视频转写和 PDF 解析的本地结构与处理状态；涉及新供应商或付费服务时单独确认。
3. 最后恢复 OpenAI API 调用，完成真实向量写入、删除、批量回填、纯向量/混合召回和引用正确率评测。
4. 续跑人格回归评测并进行人工盲评，再决定是否升级默认聊天模型。
5. 达到 `PRODUCT_PRINCIPLES.md` 的文字对话 Beta 闸门后，再进行 5–10 人知情封闭测试。
6. 公开上线前，在独立临时 Supabase 项目完成一次数据库与 Storage 的加密备份和恢复演练。
