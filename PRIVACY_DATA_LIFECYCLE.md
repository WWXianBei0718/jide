# 「记得」隐私数据生命周期

> 状态：工程型 Alpha。本文记录导出与删除边界；实际代码和 `CURRENT_STATE.md` 优先。

## 当前数据位置

| 数据 | 位置 | 当前导出 | 当前删除 |
|---|---|---|---|
| 账号与邮箱 | Supabase Auth | 结构化导出包含账号标识和邮箱 | 未完成 |
| 人物档案 | PostgreSQL `memory_profiles` | 已包含 | 删除人物接口存在 |
| 文字与材料元数据 | `memory_materials` | 已包含 | 删除材料接口存在 |
| 记忆切片与索引状态 | `memory_chunks` | 包含可读切片，不包含向量数字 | 随材料数据库级联删除 |
| 聊天记录 | `messages` / `conversations` | 已包含 | 随人物级联删除 |
| 私有文件 | Supabase Storage | 可用文件可随 ZIP 压缩包导出；单次上限 100 个或 100MB | 单项材料删除已接入；账号级清理未完成 |
| 授权证明 | `consents` | 已包含 | 账号级清理未完成 |
| 声音模型 | ElevenLabs + 本地 `voice_id` | 导出本地标识和任务记录 | 供应商侧完整删除未完成 |
| 调用保护记录 | `chat_usage_events` | 已包含时间记录 | 数据库支持按账号级联删除 |

## 完整导出验收

- 结构化 JSON 必须由登录用户本人请求，响应禁止缓存。
- 数据库记录必须分页读取，不能因默认 1000 行限制静默缺失。
- ZIP 版会把用户当前可用的私有文件打包，逐项核对大小和已记录的 SHA-256，并提供校验清单。
- 当前同步压缩包单次上限为 100 个文件或 100MB；超过限制时必须明确失败，后续由异步大容量导出替代。
- 导出失败时整体失败，不能把不完整包标记为成功。
- 导出内容不得包含服务密钥、Storage 内部路径或不可读的向量数字。

## 完整删除顺序

1. 重新认证用户，并要求输入明确确认文本。
2. 冻结新的上传、聊天和声音克隆操作。
3. 删除或停用 ElevenLabs 声音模型，并记录供应商确认结果。
4. 删除 Supabase 私有 Storage 中的正式文件和隔离文件。
5. 删除人物、资料、切片、聊天、授权和任务记录。
6. 删除 Supabase Auth 账号并使所有会话失效。
7. 返回不含隐私正文的删除回执；失败项进入可重试清理任务。

## 发布阻断项

- 未完成供应商声音删除确认前，不提供“一键删除成功”的误导提示。
- 任何部分失败时不得把账号标记为完全删除。
- 生产发布前必须验证：跨用户无法导出、已删除文件无法通过旧链接访问、删除后无法继续使用旧会话。
